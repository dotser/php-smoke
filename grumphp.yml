parameters:
  bin_dir: "./vendor/bin"
  git_dir: "."
  tasks:
    phpcs:
      standard: "./phpcs.xml"
      ignore_patterns:
      - PSR2.Namespaces.NamespaceDeclaration.BlankLineAfter
    phpspec: ~
    phpunit: ~
