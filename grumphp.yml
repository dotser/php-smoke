parameters:
  bin_dir: "./vendor/bin"
  git_dir: "."
  tasks:
    phpcs:
      standard: PSR2
      ignore_patterns:
      - PSR2.Namespaces.NamespaceDeclaration.BlankLineAfter
    phpspec: ~
    phpunit: ~
